# Hezil Barajı ve HES — Python bağımlılıkları
# Kurulum:  pip install -r requirements.txt
#
# scipy opsiyoneldir: kot–alan–hacim ve tünel maliyeti eğrilerinde yalnızca
# PCHIP interpolasyonu için kullanılır; yoksa saf numpy yedeği devreye girer.

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.0
scipy>=1.10
python-dotenv>=1.0
# python-dotenv: pano_sunucu.py .env (kullanıcı/oturum ayarları) için gereklidir
